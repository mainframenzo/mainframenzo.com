The use of conda has been @deprecated - you've moved to pixi.

The files in this folder are responsible for defining dependencies in the conda ecosystem.

A few notes:
* Each env file corresponds to a particular tool this blog utilizes
* Env files are separated because of dependency conflicts between all the Python dependencies
